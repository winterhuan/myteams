{
  "name": "agentteams",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@agentteams/*' build",
    "test": "bun run --filter '@agentteams/*' test",
    "teams": "bun run packages/cli/src/main.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}